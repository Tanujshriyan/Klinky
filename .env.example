# Required in production — use a long random string
JWT_SECRET=change-me-to-a-long-random-secret

# Optional
JWT_EXPIRE_HOURS=168
CORS_ORIGINS=*
HOST=0.0.0.0
PORT=8000

# Optional S3 presign (leave blank for mock uploads)
S3_BUCKET=
S3_REGION=eu-west-1
S3_PUBLIC_BASE_URL=
